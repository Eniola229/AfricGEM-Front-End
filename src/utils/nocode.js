// import { extractCoverTopics } from "./helpers";

// const postLength = body.length > 1000;
// const topicList = extractCoverTopics(body);
// console.log(topicList);
// // remove (1.) from the topic list so you can use array index for number
// const filterdList = topicList
//   .map((list, i) => list.split(`${i + 1}.`))
//   .flat()
//   .filter((list) => list !== "");
// const filterdListTopic = filterdList.toString().split(":");
// console.log(filterdListTopic);

/*
 <ul className="flex flex-col gap-3 list-disc">
              {listTopics.map((list, i) => (
                <li key={i}>{list.topic}</li>
              ))}
            </ul> */
